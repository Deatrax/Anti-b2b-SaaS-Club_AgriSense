// Money card — EDITABLE (§A.2). compute_financials + posted CaaS charges move it (projected→actual).
export function Money() {
  return <div>{/* TODO: headline figures + AdjustInputs + Ledger */}</div>;
}
