// Editable inputs wired to the pure financial engine (§A.1 T0-5): change area/budget →
// every figure moves consistently. Same machinery scenario simulation reuses.
export function AdjustInputs() {
  return <div>{/* TODO: editable inputs → live recompute */}</div>;
}
