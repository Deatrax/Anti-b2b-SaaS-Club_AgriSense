// <WhyPanel> — closes the explainability gap (§A.1 T0-6). Renders a tool result's
// provenance[] + assumptions[] directly. No prompt engineering; the model can't fake a citation.
export function WhyPanel() {
  return <div>{/* TODO: inputs used · rule/table applied · evidence · therefore */}</div>;
}
