// HITL gate (§A.3, §C.7): [Approve] [Edit] [Cancel] BEFORE the debit fires. The differentiator.
export function ApprovalGate() {
  return <div>{/* TODO: approve/edit/cancel — debit blocked until approved */}</div>;
}
