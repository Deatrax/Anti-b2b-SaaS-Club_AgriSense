// Phone entry (§C.3). Phone = plain identifier, NO OTP (§A.3).
export default function Home() {
  return (
    <main>
      <h1>AgriSense AI</h1>
      {/* TODO: phone input → POST /api/auth/login → redirect to /farm */}
    </main>
  );
}
