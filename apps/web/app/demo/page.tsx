// Forced 3-column projector route (§C.3): chat · trace · evidence. For the live demo only.
export default function DemoPage() {
  return <main>{/* TODO: fixed 3-column layout for the projector */}</main>;
}
