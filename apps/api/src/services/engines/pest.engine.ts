// engines/pest.engine.ts — PURE. Deterministic if-then from pest_rules.json thresholds × forecast.
export function assessPestRisk(_input: unknown): unknown {
  // TODO: match crop × stage × (temp/RH windows) against the 16-day forecast → RiskWindow rows.
  throw new Error('assessPestRisk not implemented');
}
