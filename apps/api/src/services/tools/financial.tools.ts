// tools/financial.tools.ts — compute_financials, simulate_scenario.
// Class: 'deterministic'. Both share the pure financial engine (§C.9) — scenario = mutated inputs.
export function registerFinancialTools(): void {
  // TODO: register compute_financials + simulate_scenario (→ financial.engine, persist scenario_runs).
}
