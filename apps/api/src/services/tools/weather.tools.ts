// tools/weather.tools.ts — get_weather. Class: 'external'. The ONE required real call (§1.2 #2).
// Uses openmeteo.client; provenance.source = "Open-Meteo (ECMWF)"; fallback = disk cache.
export function registerWeatherTools(): void {
  // TODO: register get_weather (PLANNING, MAINTAINING) with a cached-forecast fallback.
}
