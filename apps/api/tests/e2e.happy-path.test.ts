// e2e happy path — the walking skeleton (§C.10 H8). Keep this green before adding anything.
import { describe, it } from 'vitest';

describe('e2e happy path (§C.10 H8 walking skeleton)', () => {
  it.todo('vague opener → targeted follow-up → real weather → ranked crops → dated plan → costed projection, with a live trace');
});
