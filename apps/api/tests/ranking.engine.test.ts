// ranking.engine tests (§C.9 + §3 "verify grounding is real").
import { describe, it } from 'vitest';
// import { rankCrops } from '../src/services/engines/ranking.engine';

describe('ranking.engine (§C.9)', () => {
  it.todo('returns ≥3 ranked candidates, each with sub-scores + the weights');
  it.todo('two different farm profiles produce different rankings (grounding is real)');
});
